# Migration Transform Scripts

Contains deterministic transformation scripts:
- Foreign-key mapping and translation (`id_map.json`)
- Date format normalization to ISO-8601 / Unix timestamps
- Legacy sales alias alignment
